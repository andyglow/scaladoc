-# --
/** = Human =
  * A class to represent a ''human being''.
  *
  * Specify the `name`, `age`, and `weight` when creating a new `Person`,
  * then access the fields like this:
  *
  * {{{
  * val p = Person("Al", 42, 200.0)
  * p.name
  * p.age
  * p.weight
  * }}}
  *
  * Did you know: The [[com.acme.foo.Employee]] extends this class.
  *
  * @constructor Create a new person with a `name`, `age`, and `weight`.
  * @param name The person's name.
  * @param age The person's age.
  * @param weight The person's weight.
  * @author Alvin Alexander
  * @version 1.0
  * @todo Add more functionality.
  * @see See [[http://alvinalexander.com alvinalexander.com]] for more information.
  */
-# --
scaladoc
  [description]
    |<h1> Human </h1>
    |<p>
    |  A class to represent a <i>human being</i>.
    |</p>
    |<p>
    |  Specify the <pre>name</pre>, <pre>age</pre>, and <pre>weight</pre> when creating a new <pre>Person</pre>,
    |   then access the fields like this:
    |</p>
    |<code>
    |   val p = Person("Al", 42, 200.0)
    |   p.name
    |   p.age
    |   p.weight
    |</code>
    |<p>
    |  Did you know: The <a>com.acme.foo.Employee</a> extends this class.
    |</p>
  [constructor]
    |Create a new person with a <pre>name</pre>, <pre>age</pre>, and <pre>weight</pre>.
  [param: name]
    |The person's name.
  [param: age]
    |The person's age.
  [param: weight]
    |The person's weight.
  [author: Alvin Alexander]
  [version: 1.0]
  [todo]
    |Add more functionality.
  [see]
    |See <a>http://alvinalexander.com alvinalexander.com</a> for more information.
-# --